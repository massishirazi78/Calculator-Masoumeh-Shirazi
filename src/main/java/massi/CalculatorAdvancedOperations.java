package massi;

/*
 * I den klass finns 10 metoder av Advanceed Operation sin,cos,tan,cotan,modulu,log ,sqrt,Pi,e,exp.
 * 
 * 
 */
public class CalculatorAdvancedOperations {
	/*
	 * Den metod räknar sin FirstNumber.
	 * 
	 * @param FirstNumber int
	 * 
	 * 
	 * @param return double värde av resultat from sin(FirstNumber)
	 */

	public double sin(int FirstNumber) {
		double n = Math.sin(FirstNumber);
		return n;
	}
	/*
	 * Den metod räknar sin FirstNumber.
	 * 
	 * @param FirstNumber double
	 * 
	 * @param return double värde av resultat from sin(FirstNumber)
	 */

	public double sin(double FirstNumber) {
		double n = Math.sin(FirstNumber);
		return n;
	}
	/*
	 * Den metod räknar sin FirstNumber.
	 * 
	 * @param FirstNumber short
	 * 
	 * @param return double värde av resultat from sin(FirstNumber)
	 */

	public double sin(short FirstNumber) {
		double n = Math.sin(FirstNumber);
		return n;
	}
	/*
	 * Den metod räknar sinous FirstNumber.
	 * 
	 * @param FirstNumber long
	 * 
	 * @param return double värde av resultat from sin(FirstNumber)
	 */

	public double sin(long FirstNumber) {
		double n = Math.sin(FirstNumber);
		return n;
	}
	/*
	 * Den metod räknar cosinous FirstNumber.
	 * 
	 * @param FirstNumber integer
	 * 
	 * @param return double värde av resultat from cos(FirstNumber)
	 */

	public double cos(int FirstNumber) {
		double n = Math.cos(FirstNumber);
		return n;
	}
	/*
	 * Den metod räknar cos FirstNumber.
	 * 
	 * @param FirstNumber double
	 * 
	 * @param return double värde av resultat from cos(FirstNumber)
	 */

	public double cos(double FirstNumber) {
		double n = Math.cos(FirstNumber);
		return n;
	}
	/*
	 * Den metod räknar cos FirstNumber.
	 * 
	 * @param FirstNumber short
	 * 
	 * @param return double värde av resultat from cos(FirstNumber)
	 */

	public double cos(short FirstNumber) {
		double n = Math.cos(FirstNumber);
		return n;
	}
	/*
	 * Den metod räknar cos FirstNumber.
	 * 
	 * @param FirstNumber long
	 * 
	 * @param return double värde av resultat from cos(FirstNumber)
	 */

	public double cos(long FirstNumber) {
		double n = Math.cos(FirstNumber);
		return n;
	}

	/*
	 * Den metod räknar tan FirstNumber.
	 * 
	 * @param FirstNumber integer
	 * 
	 * @param return double värde av resultat from tan(FirstNumber)
	 */
	public double tan(int FirstNumber) {
		double n = Math.tan(FirstNumber);
		return n;
	}
	/*
	 * Den metod räknar tan FirstNumber.
	 * 
	 * @param FirstNumber double
	 * 
	 * @param return double värde av resultat from tan(FirstNumber)
	 */

	public double tan(double FirstNumber) {
		double n = Math.tan(FirstNumber);
		return n;
	}
	/*
	 * Den metod räknar tan FirstNumber.
	 * 
	 * @param FirstNumber short
	 * 
	 * @param return double värde av resultat from tan(FirstNumber)
	 */

	public double tan(short FirstNumber) {
		double n = Math.tan(FirstNumber);
		return n;
	}
	/*
	 * Den metod räknar tan FirstNumber.
	 * 
	 * @param FirstNumber long
	 * 
	 * @param return double värde av resultat from tan(FirstNumber)
	 */

	public double tan(long FirstNumber) {
		double n = Math.tan(FirstNumber);
		return n;
	}

	/*
	 * Den metod räknar cotan FirstNumber.
	 * 
	 * @param FirstNumber integer
	 * 
	 * @param return double värde av resultat from cotan(FirstNumber)
	 */
	public double cotan(int FirstNumber) {
		double n = Math.log(FirstNumber);
		return n;
	}
	/*
	 * Den metod räknar cotan FirstNumber.
	 * 
	 * @param FirstNumber double
	 * 
	 * @param return double värde av resultat from cotan(FirstNumber)
	 */

	public double cotan(double FirstNumber) {
		double n = Math.log(FirstNumber);
		return n;
	}
	/*
	 * Den metod räknar cotan FirstNumber.
	 * 
	 * @param FirstNumber short
	 * 
	 * @param return double värde av resultat from cotan(FirstNumber)
	 */

	public double cotan(short FirstNumber) {
		double n = Math.log(FirstNumber);
		return n;
	}
	/*
	 * Den metod räknar cotan FirstNumber.
	 * 
	 * @param FirstNumber long
	 * 
	 * @param return double värde av resultat from cotan(FirstNumber)
	 */

	public double cotan(long FirstNumber) {
		double n = Math.log(FirstNumber);
		return n;
	}

	/*
	 * Den metod räknar sqrt FirstNumber.
	 * 
	 * @param FirstNumber integer
	 * 
	 * @param return double värde av resultat from sqrt(FirstNumber)
	 */

	public double sqr(int FirstNumber) {
		double n = Math.sqrt(FirstNumber);
		return n;
	}
	/*
	 * Den metod räknar sqrt FirstNumber.
	 * 
	 * @param FirstNumber double
	 * 
	 * @param return double värde av resultat from sqrt(FirstNumber)
	 */

	public double sqr(double FirstNumber) {
		double n = Math.sqrt(FirstNumber);
		return n;
	}
	/*
	 * Den metod räknar sqrt FirstNumber.
	 * 
	 * @param FirstNumber short
	 * 
	 * @param return double värde av resultat from sqrt(FirstNumber)
	 */

	public double sqr(short FirstNumber) {
		double n = Math.sqrt(FirstNumber);
		return n;
	}
	/*
	 * Den metod räknar sqrt FirstNumber.
	 * 
	 * @param FirstNumber long
	 * 
	 * @param return double värde av resultat from sqrt(FirstNumber)
	 */

	public double sqr(long FirstNumber) {
		double n = Math.sqrt(FirstNumber);
		return n;
	}

	/*
	 * Den metod räknar PI * FirstNumber.
	 * 
	 * @param FirstNumber integer
	 * 
	 * @param return double värde av resultat from PI*FirstNumber.
	 */
	public double PI(int FirstNumber) {
		double n = (Math.PI) * FirstNumber;
		return n;
	}
	/*
	 * Den metod räknar PI*FirstNumber.
	 * 
	 * @param FirstNumber double
	 * 
	 * @param return double värde av resultat from PI*FirstNumber.
	 */

	public double PI(double FirstNumber) {
		double n = (Math.PI) * FirstNumber;
		return n;
	}
	/*
	 * Den metod räknar PI* FirstNumber.
	 * 
	 * @param FirstNumber short
	 * 
	 * @param return double värde av resultat from PI*FirstNumber.
	 */

	public double PI(short FirstNumber) {
		double n = (Math.PI) * FirstNumber;
		return n;
	}
	/*
	 * Den metod räknar cos FirstNumber.
	 * 
	 * @param FirstNumber long
	 * 
	 * @param return double värde av resultat from PI*FirstNumber
	 */

	public double PI(long FirstNumber) {
		double n = (Math.PI) * FirstNumber;
		return n;
	}
	/*
	 * Den metod räknar e*FirstNumber.
	 * 
	 * @param FirstNumber integer
	 * 
	 * @param return double värde av resultat from e*FirstNumber
	 */

	public double E(int FirstNumber) {
		double n = Math.E * FirstNumber;
		return n;
	}
	/*
	 * Den metod räknar e*FirstNumber.
	 * 
	 * @param FirstNumber double
	 * 
	 * @param return double värde av resultat from e*FirstNumber
	 */

	public double E(double FirstNumber) {
		double n = Math.E * FirstNumber;
		return n;
	}
	/*
	 * Den metod räknar e*FirstNumber.
	 * 
	 * @param FirstNumber short
	 * 
	 * @param return double värde av resultat from e*FirstNumber
	 */

	public double E(short FirstNumber) {
		double n = Math.E * FirstNumber;
		return n;
	}
	/*
	 * Den metod räknar e*FirstNumber.
	 * 
	 * @param FirstNumber long
	 * 
	 * @param return double värde av resultat from e*FirstNumber
	 */

	public double E(long FirstNumber) {
		double n = Math.E * FirstNumber;
		return n;
	}
	/*
	 * Den metod räknar exp(FirstNumber).
	 * 
	 * @param FirstNumber integer
	 * 
	 * @param return double värde av resultat from exp(FirstNumber)
	 */

	public double EXP(int FirstNumber) {
		double n = Math.exp(FirstNumber);
		return n;
	}
	/*
	 * Den metod räknar exp(FirstNumber).
	 * 
	 * @param FirstNumber double
	 * 
	 * @param return double värde av resultat from exp(FirstNumber)
	 */

	public double EXP(double FirstNumber) {
		double n = Math.exp(FirstNumber);
		return n;
	}
	/*
	 * Den metod räknar exp(FirstNumber).
	 * 
	 * @param FirstNumber short
	 * 
	 * @param return double värde av resultat from exp(FirstNumber)
	 */

	public double EXP(short FirstNumber) {
		double n = Math.exp(FirstNumber);
		return n;
	}
	/*
	 * Den metod räknar exp(FirstNumber).
	 * 
	 * @param FirstNumber long
	 * 
	 * @param return double värde av resultat from exp(FirstNumber).
	 */

	public double EXP(long FirstNumber) {
		double n = Math.exp(FirstNumber);
		return n;
	}

	/*
	 * Den metod räknar log(FirstNumber).
	 * 
	 * @param FirstNumber integer
	 * 
	 * @param return double värde av resultat from log(FirstNumber)
	 */
	public double LOG(int FirstNumber) {
		double n = Math.log(FirstNumber);
		return n;
	}
	/*
	 * Den metod räknar log(FirstNumber).
	 * 
	 * @param FirstNumber double.
	 * 
	 * @param return double värde av resultat from log(FirstNumber)
	 */

	public double LOG(double FirstNumber) {
		double n = Math.log(FirstNumber);
		return n;
	}
	/*
	 * Den metod räknar log(FirstNumber).
	 * 
	 * @param FirstNumber long
	 * 
	 * @param return double värde av resultat from log(FirstNumber)
	 */

	public double LOG(long FirstNumber) {
		double n = Math.log(FirstNumber);
		return n;
	}
	/*
	 * Den metod räknar log(FirstNumber).
	 * 
	 * @param FirstNumber short
	 * 
	 * @param return double värde av resultat from log(FirstNumber)
	 */

	public double LOG(short FirstNumber) {
		double n = Math.log(FirstNumber);
		return n;
	}

	/*
	 * Den metod räknar modulus av FirstNumber och SecondNumber sedan return result double
	 * värde av FirstNumber % SecondNumber.
	 * 
	 * @param FirstNumber integer
	 * 
	 * @param SecondNumber integer
	 * 
	 * @param return double värde av resultat from FirstNumber % SecondNumber.
	 */

	public double modulus(int FirstNumber, int SecondNumber) {

		return (FirstNumber % SecondNumber);
	}
	/*
	 * Den metod räknar modulus av FirstNumber och SecondNumber sedan return result double
	 * värde av FirstNumber % SecondNumber.
	 * 
	 * @param FirstNumber double
	 * 
	 * @param SecondNumber double
	 * 
	 * @param return double värde av resultat from FirstNumber % SecondNumber.
	 */

	public double modulus(double FirstNumber, double SecondNumber) {

		return (FirstNumber % SecondNumber);
	}
	/*
	 * Den metod räknar modulus av FirstNumber och  SecondNumber return result double
	 * värde av FirstNumber % SecondNumber.
	 * 
	 * @param FirstNumber short
	 * 
	 * @param SecondNumber short
	 * 
	 * @param return double värde av resultat from FirstNumber % SecondNumber.
	 */

	public double modulus(short FirstNumber, short SecondNumber) {

		return (FirstNumber % SecondNumber);
	}
	/*
	 * Den metod räknar modulus av FirstNumber och SecondNumber sedan return result double
	 * värde av FirstNumber % SecondNumber.
	 * 
	 * @param FirstNumber long
	 * 
	 * @param SecondNumber long
	 * 
	 * @param return double värde av resultat from FirstNumber % SecondNumber.
	 */

	public double modulus(long FirstNumber, long SecondNumber) {

		return (FirstNumber % SecondNumber);
	}
}
/*
 * slut av CalculatorAdvancedOperations class
 */
